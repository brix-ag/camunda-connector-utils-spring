package ch.brix.camunda.connector.util.gson;

/**
 * JSON classes with a special String representation.
 * Those classes need to implement...
 * 1. a public constructor(String)
 * 2. toString() should give the JSON representation
 */
public interface StringLike {
}
